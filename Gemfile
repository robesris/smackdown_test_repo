source 'https://rubygems.org'

gem 'rake'
gem 'minitest'
gem 'simplecov', "~> 0.9.1", :require => false
gem 'simplecov-json', :require => false

# smackdown_test_repo should only be used as a submodule in the smackdown repo, hence the unusual gem path here
gem 'smackdown', path: File.join(File.dirname(__FILE__), '..', '..')
